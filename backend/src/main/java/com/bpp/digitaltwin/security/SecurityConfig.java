package com.bpp.digitaltwin.security;

import jakarta.enterprise.context.ApplicationScoped;

/**
 * Placeholder for centralizing security-related configuration
 * (JWT issuer/audience validation, role mapping, etc.). No endpoints are
 * secured yet in Phase 2 — all /api/** routes are open for local development.
 */
@ApplicationScoped
public class SecurityConfig {
}

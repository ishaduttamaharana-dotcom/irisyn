# Template only — do NOT commit real values. Populate via `oc create secret`,
# a sealed-secrets/external-secrets pipeline, or the OpenShift console.
apiVersion: v1
kind: Secret
metadata:
  name: digital-twin-backend-secret
type: Opaque
stringData:
  DB_USERNAME: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"

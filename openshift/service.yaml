apiVersion: v1
kind: Service
metadata:
  name: digital-twin-backend
  namespace: digital-twin
  labels:
    app: digital-twin-backend
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: digital-twin-backend
  type: ClusterIP

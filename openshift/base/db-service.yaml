apiVersion: v1
kind: Service
metadata:
  name: digital-twin-db
  labels:
    app: digital-twin-db
spec:
  selector:
    app: digital-twin-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
